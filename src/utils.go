package main

import (
	"debug/pe"
)

// getFileContents reads and returns the content of the file at the given path.
func getDosHeader(peFile *pe.File) (string, error) {
	return "not implemented", nil
}
